spring.application.name: demo

client-openai:
  key: ${OPENAI_API_KEY}
  endpoint: ${OPENAI_API_ENDPOINT}
  deployments-list-path: /openai/deployments
  deployment-name: ${OPENAI_DEPLOYMENT_NAME}

weather-api:
  key: ${WEATHER_API_KEY}
  endpoint: ${WEATHER_API_ENDPOINT}